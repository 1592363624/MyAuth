# 2026-08-02

## 本地调试 + 配置外置改造

### 本地跑通前后端
- Redis 用用户桌面的 `C:\Users\15923\Desktop\Redis-x64-5.0.14.1` 绿色版启动。
- MySQL80 服务需管理员权限启动；root 密码为 `root`；建库 `myauthtest`/用户 `myauthtest`(密码同)，导入 `doc/myauth.sql`（24 张表）。
- 后端用本机 JDK17（`C:\Program Files\Java\jdk-17.0.4.1`）编译运行，与服务器一致。修复：Lombok 锁 1.18.34 + compiler 插件 annotationProcessor（兼容 JDK17/23）；`EpayOrders extends Model<EpayOrders>`。
- 前端 npm install 后 `npm run serve` 跑在 8001。
- 默认管理员账号 `admin`/`123456`（InitConfig 启动时初始化，ma_admin 表空时插入）。

### 配置外置（用户核心诉求：生产配置不泄露、不被 jar 覆盖）
- `serverConfig.json` 原本写死 8080 导致登录失败，已还原用户生产值 `https://my.52shell.ltd`。
- 前端 `main.js` 改为开发环境用 `.env` 的 `VUE_APP_LocalRootUrl`(8081)，生产才读 `serverConfig.json`。
- `application-prod.yml` 与 `serverConfig.json` 已从 Git 移除并加入 `.gitignore`，改为提交 `.example` 模板。
- **重要修正（下午）**：用户确认服务器外部配置实际路径为 `C:\wwwroot\myauth\config\application.yml`（Spring Boot 标准 config 子目录约定，文件名不带 -prod 后缀，作为主配置直接用，不依赖 profile）。
  - CI 部署脚本已改为生成/检查 `config\application.yml`（首次用 MYAUTH_* Secrets 生成，已存在则跳过）。
  - 模板文件已改名 `application-prod.example.yml` → `config/application.example.yml`，gitignore 同步更新（`myauth/src/main/resources/config/application.yml` 被忽略，模板强制放行）。
  - 本地残留旧 `application-prod.yml` 已删除，避免误打包。
- CI 工作流改造：前端 build 用 `FRONTEND_API_URL` Secret 生成 serverConfig.json；SSH 部署脚本确保服务器外部 `config\application.yml` 存在（存在则跳过，不覆盖）。
- 遗留：GitHub 远程历史仍含旧真实文件，若仓库公开需 `git filter-repo` 清理。

### 方案再次简化（用户选择去掉模板，CI 直接覆盖）—— 已 REVERT
- 用户一度认为本地配置写测试数据直接打包、CI 用 Secrets 覆盖即可，不需要模板。但随后改回模板方案：
  **用户顾虑：本地开发配置可能含敏感数据，所以还是只上传 .example 模板到 GitHub，本地两个真实配置文件不入库。**

### 最终方案（模板 + 本地配置不入库）
- 已执行（revert 回模板方案）：
  - 删除本地真实 `config/application.yml`（不入库）；重建 `config/application.example.yml` 模板（含 `${MYAUTH_*}` 占位符 + 本地默认值）。
  - 重建 `serverConfig.example.json` 模板。
  - `.gitignore`：忽略真实文件 `config/application.yml`、`serverConfig.json`，强制放行两个 `.example` 模板。已验证：真实文件被忽略、模板未被忽略可正常提交。
  - CI 后端步骤改回"防御性删除真实 config/application.yml"（仓库仅含模板，jar 内为占位符默认值；生产配置由服务器外部 `C:/wwwroot/myauth/config/application.yml` 提供，重传 jar 不覆盖）。
  - 前端 build 仍用 `FRONTEND_API_URL` Secret 覆盖 `serverConfig.json`。
  - SSH 部署脚本保留"服务器外部 config\application.yml 存在则跳过"兜底（外部优先）。

### Secrets 方案改为整文件（方案 A，用户决定）
- 用户认为拆成十几个 MYAUTH_* 变量太麻烦，要求把整个文件内容作为一个 Secret。
- 已执行：
  - 新增 2 个整文件 Secret：`BACKEND_CONFIG_YML`（整个 config/application.yml 内容）、`FRONTEND_CONFIG_JSON`（整个 serverConfig.json 内容）。
  - 删除所有拆散变量：`MYAUTH_DB_*`/`REDIS_*`/`MAIL_*`/`GEN_KEY`、`FRONTEND_API_URL`、`FRONTEND_ICP` 全部从 CI 移除。
  - 前端 build：用 `FRONTEND_CONFIG_JSON` 整文件 Secret 直接 `cat > public/serverConfig.json` 覆盖（未配置则保留仓库默认）。
  - SSH 脚本 0 号步骤：经 ssh-action `env:` 映射 `BACKEND_CONFIG_YML` 为远程环境变量；PowerShell 用 `[System.IO.File]::WriteAllText(..., UTF8 BOM)` 写出 `config\application.yml`（方案A=始终覆盖；未配置则保留服务器现有文件）。
  - 注意：GitHub Secrets 单条上限 64KB，两配置文件远小于此，安全。

### 部署配置文档化（README）
- 新建 `README-部署配置.md` 入库：记录 CI 流程、部署后会覆盖的文件（前端 serverConfig.json、后端 config/application.yml、jar、dist）、后端重启脚本健壮性保障（备份/回滚）、生产路径对照、8 个必填 Secrets 清单及填写注意事项、注意事项 Checklist。
- 明确：当前工作流仅需 8 个 Secrets（BACKEND_CONFIG_YML、BACKEND_REMOTE_DIR、FRONTEND_CONFIG_JSON、FRONTEND_REMOTE_DIR、WIN_HOST、WIN_PORT、WIN_USER、WIN_SSH_KEY）；`FRONTEND_API_URL` 已弃用，勿再配置。
- 强调：整文件 Secret 是"始终覆盖"策略，Secret 内容必须 = 服务器当前可用配置。

### 用户偏好
- 服务器配置必须保留、不能被覆盖，优先用"jar 外部配置文件"方案。
- 本地调试端口 8081，服务器实际 6401（宝塔 `--server.port=6401`）。
